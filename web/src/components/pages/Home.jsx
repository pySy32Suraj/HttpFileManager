import React from 'react'

import ConnectionView from '../view/ConnectionView';

const Home = ({connections}) => {
  return (
    <div>
      {
        // connections.map(connection => (
        //   <ConnectionView />
        // ))
      }
    </div>
  )
}

export default Home